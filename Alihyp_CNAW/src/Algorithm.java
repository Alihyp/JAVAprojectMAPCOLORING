public class Algorithm {
    /* for coloring each node program must check if the used color is in
    neighbores or not
    method (color){
        for (i : node.neibores){
            if color == i.color
                reise eror
            else
                node.color = color
    }

   }

     */
}
